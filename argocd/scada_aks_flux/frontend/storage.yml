apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: scadafrontend-data
  namespace: scada
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: default
  resources:
    requests:
      storage: 5Gi
